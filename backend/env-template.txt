# Backend Environment Variables
# Copy this content to backend/.env file

PORT=5000

# Stripe Secret Key
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here

# Supabase Configuration (Required)
SUPABASE_URL=your_supabase_project_url
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Frontend URL (for redirects after payment)
FRONTEND_URL=http://localhost:3000

# OpenAI API Key (Optional - for AI insights)
# OPENAI_API_KEY=your_openai_api_key_here

# BLS API Key (Optional - for higher rate limits)
# Register at https://www.bls.gov/developers/api_signature_v2.htm
# BLS_API_KEY=your_bls_api_key_here

# CDC API Key (Optional - for CDC Wonder API access)
# Register at https://wonder.cdc.gov/ if needed
# CDC_API_KEY=your_cdc_api_key_here

# SAM.gov API Key (Optional - required to use /api/example/samgov/live)
# Get it from your SAM.gov Account Details (Public API Key)
# SAM_GOV_API_KEY=your_sam_gov_api_key_here

# CORS Allowed Origins
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Email notifications (messages, likes, saves, comments) via Resend
# Get API key at https://resend.com — if unset, notifications are skipped (logged in dev).
# RESEND_API_KEY=re_xxxxxxxxxxxx
# Sender shown in inbox (use a verified domain in production)
# NOTIFICATION_EMAIL_FROM=Analytics Shorts <notifications@yourdomain.com>


